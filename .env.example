# Frontend Environment Variables

# API URL - adjust for your environment
NEXT_PUBLIC_API_URL=http://localhost:8000

# GitHub Integration - Username to display on GitHub page
NEXT_PUBLIC_GITHUB_USERNAME=your-github-username

# GitHub API Token - Personal access token for higher rate limits (5000/hour vs 60/hour)
# Create token at: https://github.com/settings/tokens/new
# Required scopes: public_repo (read-only access to public repositories)
# Without token: 60 requests/hour (will hit rate limit quickly)
# With token: 5000 requests/hour (recommended)
NEXT_PUBLIC_GITHUB_TOKEN=

# Node Environment
NODE_ENV=development

# Optional: Analytics
NEXT_PUBLIC_GA_ID=

# Optional: Sentry
NEXT_PUBLIC_SENTRY_DSN=
